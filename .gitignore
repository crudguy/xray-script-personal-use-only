# ==========================================================================
# WorkBuddy 工作区（私有数据，非项目源码，勿提交）
# 含 .workbuddy/memory 笔记、.workbuddy/reports 分析产物
# 注: 测试临时目录原先也在这里, 已于 2026-09-21 迁出, 见下方 test/.tmp/
# ==========================================================================
.workbuddy/

# ==========================================================================
# 测试临时目录（test/ 下各用例的沙箱，运行结束自动清理）
# 刻意用"仓库内固定目录"而非 mktemp: Windows/Git-Bash 下 mktemp 可能返回
# C:\... 反斜杠路径, 会污染 PATH、破坏 awk -v 转义与 chmod +x 的路径解析。
# ==========================================================================
test/.tmp/

# 测试框架的安全删除回收站（safe-delete 运行时在 CWD 生成，纯测试产物）
.Trash-0/

# ==========================================================================
# 日志
# ==========================================================================
*.log

# ==========================================================================
# 本地 / 运行时配置（可能含真实凭据，勿提交）
# config.json 本身是空模板，保留纳入版本控制；以下为运行时生成的本地副本
# ==========================================================================
.env
.env.*
config.local.json
*.local.json

# ==========================================================================
# 操作系统 / 编辑器噪音
# ==========================================================================
.DS_Store
Thumbs.db
Desktop.ini
*.swp
*~
